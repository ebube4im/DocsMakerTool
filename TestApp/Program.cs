using System.Text;

namespace TestApp
{
    internal class Program
    {
        static void Main(string[] args)
        {

          //  //FileInfo f = new FileInfo("");

          //  //FileStream f1 = f.Open(FileMode.Open, FileAccess.Read, FileShare.None);

          //  //FileStream fs = f.OpenWrite();

          //  using (StreamReader sr = File.OpenText("myMessage.txt"))
          //  {
          //      string text = null;
          //      while ((text = sr.ReadLine()) != null)
          //      {
          //          Console.WriteLine(text);
          //      }
          //  }

          ////  using(BinaryWriter br = (File.Create("Msg.dat",0)))

          //  using (FileStream fs = File.Open("myMessage.txt", FileMode.Create))
          //  {
          //      string msg = "ABCDEFGHIJKLllMNOPQRSTUVWXYZ";
          //      byte[] msgAsByteArray = Encoding.Default.GetBytes(msg);

          //      fs.Write(msgAsByteArray,0, msgAsByteArray.Length);

          //      fs.Position = 0;

                

          //      byte[] bytsfromFIle = new byte[msgAsByteArray.Length];

          //      for (int i = 0; i < msgAsByteArray.Length; i++)
          //      {
          //          bytsfromFIle[i] = (byte)fs.ReadByte();
          //          Console.WriteLine(bytsfromFIle[i]);
          //      }
                
          //      Console.WriteLine("\n Decoded" + Encoding.Default.GetString(bytsfromFIle));
          //  }



            //f.CreateText();

            //File.CreateText("");



           // Console.WriteLine("Hello, World!");
        }
    }
}